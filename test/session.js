'use strict';

// load required mocks
var mocks = require('./support/mocks.js');

var assert = require('assert');

var Session = require('../src/session.js');

describe('Session', function() {
  it('should should also be thoroughly tested');
});
