# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# https://www.npmjs.org/doc/misc/npm-faq.html#should-i-check-my-node_modules-folder-into-git
node_modules

# Builds should be stored in a dist directory. They should NOT be included in the repo if
# it's possible.  Sometimes it's not possible e.g. for web libraries that need to store
# the release in repo.
# dist # need to include build in case downstream isn't browserified

# Prevent repo clutter
.DS_Store
Thumbs.db

# For development
test.html

.env
